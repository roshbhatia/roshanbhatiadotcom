npm run typecheck
npm run test:unit

# Blog post prose has no other backup, so say out loud when a commit touches it.
if git diff --cached --name-only | grep -q "writing/.*post.md"; then
  echo ""
  echo "NOTE: this commit changes blog post content:"
  git diff --cached --name-only | grep "writing/.*post.md"
  echo ""
fi
